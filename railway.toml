# Used when Railway Root Directory is empty (monorepo root).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No healthcheck — previous Railway healthchecks were failing cold starts.
restartPolicyType = "ON_FAILURE"
